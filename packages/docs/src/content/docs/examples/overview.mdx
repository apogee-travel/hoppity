---
title: Examples Overview
description: Runnable example projects demonstrating hoppity patterns.
---

import { LinkCard, CardGrid } from "@astrojs/starlight/components";

Hoppity ships with example projects in the `examples/` directory. Each demonstrates a different messaging pattern. All examples work with the `docker-compose.yml` at the repo root for RabbitMQ.

## Prerequisites

All examples require:

- Node.js 22+
- pnpm 9+
- Docker (for RabbitMQ)

```bash
# From the repo root
docker compose up -d    # Start RabbitMQ
pnpm install            # Install all workspace dependencies
```

## Pattern Comparison

| Pattern         | Example      | When to Use                                                     |
| --------------- | ------------ | --------------------------------------------------------------- |
| Pub/Sub         | basic-pubsub | Simple event broadcasting with raw topology escape hatch        |
| Contract-Driven | bookstore    | Multiple services with shared domain contracts, typed messaging |

<CardGrid>
    <LinkCard
        title="Basic Pub/Sub"
        href="/hoppity/examples/basic-pubsub/"
        description="Publisher sends to exchange, subscriber consumes from queue."
    />
    <LinkCard
        title="Bookstore"
        href="/hoppity/examples/bookstore/"
        description="Full contract-driven multi-service demo with events, RPC, and middleware."
    />
</CardGrid>
